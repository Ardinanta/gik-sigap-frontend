import { LoaderCircle, SearchX, type LucideIcon } from 'lucide-react'
import type { ReactNode } from 'react'
export function PageHeader({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children?: ReactNode }) { return <header className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><span className="text-xs font-semibold uppercase tracking-[.08em] text-tide">{eyebrow}</span><h1 className="mt-1 text-2xl font-bold text-ink md:text-3xl">{title}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-ink-soft">{description}</p></div>{children}</header> }
export function MetricCard({ icon: Icon, label, value, hint }: { icon: LucideIcon; label: string; value: string; hint: string }) { return <article className="rounded-xl border border-line bg-white p-4 shadow-sm"><div className="flex items-center justify-between gap-2"><p className="text-xs text-ink-soft">{label}</p><span className="grid size-8 place-items-center rounded-lg bg-success-soft text-tide"><Icon size={16} /></span></div><strong className="mt-3 block text-xl text-ink">{value}</strong><span className="mt-1 block text-xs text-ink-soft">{hint}</span></article> }
export function QueryState({ loading, error, empty, onRetry }: { loading: boolean; error: boolean; empty?: boolean; onRetry: () => void }) { if (loading) return <div className="flex min-h-48 items-center justify-center gap-2 text-sm text-ink-soft"><LoaderCircle className="animate-spin" size={18} /> Memuat data...</div>; if (error) return <div role="alert" className="rounded-xl border border-danger/20 bg-danger-soft p-5 text-sm text-danger">Data tidak dapat dimuat. <button className="rounded px-1 font-bold hover:bg-white/60 focus-visible:outline-2" onClick={onRetry}>Coba lagi</button></div>; if (empty) return <div className="grid min-h-44 place-items-center rounded-xl border border-dashed border-line bg-white p-8 text-center"><div><SearchX className="mx-auto text-tide" /><h3 className="mt-3 font-bold">Data belum tersedia</h3><p className="mt-1 text-sm text-ink-soft">Ubah filter atau tambahkan data operasional terlebih dahulu.</p></div></div>; return null }
